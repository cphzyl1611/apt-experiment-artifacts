# PROVX R3 Feature Vocabulary and Live Route Decision Implementation Plan

> **For agentic workers:** This documentation-only plan records the bounded R3 evidence audit and contract outputs. No training, benchmark, raw-data acquisition, or official-source modification is permitted.

**Goal:** Resolve whether the authenticated ProvX artifact defines the 21 input-feature meanings and, if it does not, freeze an auditable adapted live-deployment route.

**Architecture:** Audit the shipped Zenodo archive, local paper, prior R0-R2 evidence, and only cited official DARPA repositories. Preserve the exact artifact path as Track O and specify a distinct Track L with an explicit live encoder, train/evaluation separation, reversible graph mappings, and externally controlled Phase-II randomness.

**Tech Stack:** Markdown, JSON, SHA-256, PyTorch Geometric schema terminology, read-only `rg`/`curl`/`sha256sum` checks.

**Spec:** `Prompt_2_PROVX_R3_Feature_Vocabulary_Recovery_and_Live_Route_Decision.md`

## Global Constraints

- Do not train or retrain, execute formal benchmarks, acquire DARPA raw data, execute APT actions, modify official source, or mutate binding/scoring authority.
- Do not infer feature semantics from statistics alone.
- Keep the 53 playbooks / 1796 raw actions outside detector training, tuning, calibration, feature selection, and preprocessing design.
- Do not force an adapted encoder to 21 dimensions without authenticated evidence.
- Terminal fields must state the vocabulary, packaged-checkpoint live-use status, live route, `FORMAL_EXPERIMENT_EXECUTED = NO`, next action, and `STOP = true`.

### Task 1: Evidence inventory

**Files:** Create `PROVX_R3_FEATURE_SEMANTICS_SEARCH_INVENTORY.json`.

- [ ] Record every local and official source searched, URLs/revisions/hashes, search scope, positive findings, and unresolved fields.
- [ ] Distinguish authoritative feature evidence from general provenance/eCAR semantics.
- [ ] Record that no official 21-column ordering, encoder, normalization, or raw-to-graph implementation was found.

### Task 2: Route and vocabulary decision

**Files:** Create `PROVX_R3_OFFICIAL_21_FEATURE_VOCABULARY_DECISION.json`, `PROVX_R3_EXACT_VS_ADAPTED_DEPLOYMENT_CONTRACT.md`, and `PROVX_R3_LIVE_ROUTE_DECISION_REPORT.md`.

- [ ] Freeze `OFFICIAL_21_FEATURE_VOCABULARY_NOT_AVAILABLE`.
- [ ] Set packaged direct live Mininet use to `NOT_VALIDATED` and route to `PROVX_ADAPTED_LIVE_DEPLOYMENT`.
- [ ] Keep Track O and Track L contracts disjoint and state that neither is a completed live experiment.

### Task 3: Adapted schema and governance

**Files:** Create `PROVX_R3_ADAPTED_LIVE_FEATURE_SCHEMA.json`, `PROVX_R3_TRAIN_EVAL_SEPARATION_CONTRACT.json`, and `PROVX_R3_PHASE2_SEED_POLICY.json`.

- [ ] Define a deterministic 32-column process/file/socket-aware encoder with missing and unknown handling, ordering, normalization, and a version/hash identity.
- [ ] Define permissible conceptual training sources and immutable holdout rules for the 53-playbook/1796-action evaluation set.
- [ ] Pre-register primary seed `314159` and sensitivity seeds `[271828, 161803, 42]` before any formal evaluation.

### Task 4: Verification

- [ ] Parse every new JSON file.
- [ ] Verify the adapted encoder hash and source/document hashes recorded in the inventory.
- [ ] Check all required terminal fields and confirm no training/formal benchmark command was run.
